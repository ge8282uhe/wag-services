# Porta del server (opzionale, default 3000)
PORT=3000
NODE_ENV=development

# ─── SQLite (locale, default) ────────────────────────
# Se non imposti le variabili MySQL sotto, l'app usa SQLite.
# DB_PATH=./data/database.sqlite

# ─── MySQL (Hostinger / phpMyAdmin) ──────────────────
# Imposta queste variabili su Hostinger (Environment) per usare il database MySQL.
# Nome e password li trovi in hPanel → Databases → Management.
# DB_HOST=localhost
# DB_NAME=u825008747_WAGservices
# DB_USER=u825008747_Tronco
# DB_PASSWORD=la_tua_password_mysql
